query player {
  starships: allStarships(first: 36) {
    starships {
      id
      name
      model
      length
      cargoCapacity
      hyperdriveRating
    }
  }
  people: allPeople(first: 36) {
    people {
      id
      name
      birthYear
      height
      mass
    }
  }
}
